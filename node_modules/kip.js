/**
 * Prints an error to the screen and returns true.
 * function(e, item) {
 *  if (kip.err(e)) return;
 *  }
 */
module.exports.err = function(e) {
    if (e) {
        console.error(e);
        return true;
    }
};

module.exports.ohshit = function(e) {
  if (e) {
    console.error('FATAL ERROR 🔥💀')
    console.error(e);
    process.exit(1);
  }
}

module.exports.prettyPrint = function(o) {
    console.log(JSON.stringify(o, null, 2));
}
