// react/reducers/__tests__/item.test.js

import { CLEAR_ITEM, RECEIVE_ITEM } from '../../constants/ActionTypes';

const initialState = {};

describe('cart reducer', () => {
  const firstState = initialState;

});
