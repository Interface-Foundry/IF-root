COMMIT='HEAD'

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
ROOTDIR="$DIR"/../../

cd $ROOTDIR
git fetch origin
#git reset --hard origin/dev
echo 'not resetting --hard origin/dev in dev mode because that would destroy all that i have worked for all these years'
